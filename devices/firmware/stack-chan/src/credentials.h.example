#pragma once

// Optional: copy this file to credentials.h to put the control website on an
// existing 2.4 GHz Wi-Fi network. Without it, Stack-Chan creates the access
// point "stack-chan-control" with password "stack-chan".
constexpr const char *CONTROL_WIFI_SSID = "your-wifi-name";
constexpr const char *CONTROL_WIFI_PASSWORD = "your-wifi-password";

